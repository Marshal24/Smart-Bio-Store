import AdminDashboard from "@/components/AdminDashboard";

export const metadata = {
  title: "Admin Dashboard | Smart Bio-Link Store",
};

export default function AdminPage() {
  return (
    <main>
      <AdminDashboard />
    </main>
  );
}
